/*
const mongoose = require('../db/flightDB')

    const flightSchema = new mongoose.Schema({
        flightId: {
            type: String,
            require: true
        }
    })

const Flight = mongoose.model('Flight', flightSchema)

module.exports = Flight*/